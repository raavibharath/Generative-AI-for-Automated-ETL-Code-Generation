# Generative-AI-for-Automated-ETL-Code-Generation
This project provides a Flask web application that uses a pre-trained GPT-2 model to generate ETL (Extract, Transform, Load) code based on user descriptions.
